# PROJECT STATE — سامانه پشتیبانی مشتریان

> **این فایل، حافظه پروژه است.**
> اگر چت جدیدی شروع شد یا کار در Claude Code ادامه پیدا کرد، این فایل را کامل بخوان و از آخرین وضعیت ادامه بده.
> **قانون: بعد از هر تغییر یا پیشرفت، بخش «گزارش پیشرفت» در انتهای همین فایل به‌روز شود.**

- **آخرین به‌روزرسانی:** ۱۴۰۵/۰۶/۱۱ (2026-09-02)
- **وضعیت فعلی:** فاز ۱ و ۲ + پشتیبان‌گیری/SSL/داشبورد/شخصی‌سازیِ برند — آماده تست مالک پروژه
- **مسدودکننده:** ندارد

---

## ⚠️ تقسیم پروژه به دو سامانه مستقل (تصمیم ۱۴۰۵/۰۵/۲۲)

پروژه اصلی به **دو سامانه کاملاً جدا** تقسیم شد. این مخزن فقط سامانه اول است.

| | سامانه ۱ — پشتیبانی | سامانه ۲ — انبار و پروژه |
|---|---|---|
| مخزن | `Soorin_Support` (همین) | `Soorin_Inventory` |
| محتوا | مشتری، تیکت، قرارداد، فاکتور، پرتال + گزارش | کالا، انبار، واردات، BOM، پروژه + گزارش |
| استقرار | هاست اشتراکی dpst.ir، کنار سایت وردپرسی | سرور ESXi |
| دیتابیس | مستقل | مستقل |

**این دو سامانه هیچ اتصال دیتابیسی به هم ندارند.** هیچ کلید خارجی، هیچ کوئری مشترک، هیچ همگام‌سازی خودکار.

### آنچه با این تقسیم از دست رفت (پذیرفته‌شده توسط مالک پروژه)

1. **گزارش «آمار خرابی به تفکیک قطعه و برند» حذف شد** — این گزارش ذاتاً به اتصال تیکت↔قطعه نیاز دارد.
2. **کسر خودکار موجودی هنگام تعویض قطعه در تیکت انجام نمی‌شود** — باید جداگانه در سامانه انبار ثبت شود.
3. **مشتریان در هر دو سامانه جدا وارد می‌شوند** — ستون `code` در هر دو یکسان نگه داشته شود تا تطبیق دستی ممکن باشد.

### نقاط برشی که اعمال شد

| اتصال قبلی | جایگزین |
|---|---|
| `tickets.customer_system_id` (FK) | `tickets.system_name` — متن آزاد |
| `invoice_items.item_version_id` (FK) | `invoice_items.part_code` — متن آزاد |
| `customer_system_parts.replaced_by_ticket_id` (FK) | `replaced_by_ticket_number` — متن آزاد (در سامانه ۲) |
| `warehouses/projects/customer_systems.customer_id` | به جدول `customers` **خودِ سامانه ۲** وصل شد |

---

## ۱. هدف پروژه

### هویت کسب‌وکار
- **نام:** شرکت دریاپردازشگران سورین طبرستان
- **نام لاتین / برند:** SOORIN TABARESTAN
- **لوگو:** سینه کشتی به رنگ سفید روی زمینه سرمه‌ای، همراه با نوشتار SOORIN TABARESTAN
- سرمه‌ای لوگو ≈ `#0f2d4d` → **رنگ منوی کناری از `#0b2b3f` به `#0f2d4d` تغییر کند** تا لوگو بدون حاشیه در منو بنشیند
- نسخه‌های موردنیاز لوگو: سفید (روی منو و تم شب)، سرمه‌ای تک‌رنگ (روی پس‌زمینه روشن و سربرگ فاکتور)، و نشان مربعی بدون نوشتار (favicon و منوی جمع‌شده موبایل)
- زمینه سرمه‌ای اطراف لوگو حذف/شفاف شود (کاربر اجازه داده است)
- فیروزه‌ای `#14b8a6` به‌عنوان رنگ مکمل حفظ می‌شود — با حوزه دریایی شرکت هم‌خوان است

سامانه‌ای تحت وب برای ارائه خدمات و پشتیبانی به مشتریان، با این اهداف:

1. مشتری تیکت ثبت کند، مشکل حل شود، تیکت همان‌جا بسته شود
2. اگر خدمت مشمول هزینه بود، فاکتور در همان سامانه صادر شود
3. **هدف اصلی: گزارش‌گیری آینده** — چقدر خدمات داده شده، چقدر درآمد داشته، هر مشتری چه خدماتی گرفته، بیشترین خرابی‌ها در چه زمینه‌ای بوده
4. سامانه به‌عنوان رزومه و سابقه کاری عمل کند
5. فاز بعد: مدیریت کامل کالا، انبار، واردات، قیمت تمام‌شده و پروژه‌ها (جایگزین اکسل‌های دستی فعلی)

---

## ۲. زیرساخت و استک فنی (تصمیم‌گیری‌شده)

| مورد | انتخاب | دلیل |
|---|---|---|
| میزبانی | سرور لوکال کاربر روی **ESXi** + امکان اجرا روی **هاست اشتراکی cPanel** | هر دو حالت پشتیبانی می‌شود |
| سیستم‌عامل | **Ubuntu Server 24.04 LTS** | سبک‌تر و پایدارتر از ویندوز |
| دسترسی | لوکال + دسترسی اینترنت | ممکن است بعداً به هاست خارجی منتقل شود |
| اجرا | **Docker / docker-compose** یا نصب مستقیم PHP | تفاوت فقط در فایل `.env` |
| بک‌اند | **Laravel** (PHP) | سازگاری با هاست ایران، منابع فارسی، سرعت توسعه، امنیت پیش‌فرض |
| پنل مدیریت | **Filament** | واکنش‌گرا، Tailwind، حالت شب، توسعه سریع |
| دیتابیس | **MariaDB 11** | سازگاری بالا، بکاپ ساده، اتصال آسان به نرم‌افزارهای ایرانی |
| وب‌سرور | Nginx | — |
| کش و صف | Redis | — |
| مجوزها | Spatie Laravel-Permission | دسترسی مجوزمحور و منعطف |
| PDF فارسی | **mPDF + فونت وزیرمتن** | مشکل شناخته‌شده راست‌چین؛ از ابتدا درست پیاده شود |
| تاریخ | شمسی (jalali) | — |
| زبان و جهت | فارسی، RTL | — |
| بکاپ | کرون‌جاب شبانه خودکار | — |

**گزینه‌های رد شده:** Django (نیاز به VPS، منابع کمتر در ایران) — Node+React (زمان توسعه و نگهداری بیشتر، بدون مزیت تعیین‌کننده) — PostgreSQL (بدون تفاوت محسوس در این حجم)

---

---

## ۲-ب. استقرار، مخزن کد و محیط تست

### ⚠️ اصل حاکم: پروژه باید «دوحالته» ساخته شود
کد باید بدون هیچ تغییری هم روی سرور ESXi با Docker و هم روی هاست اشتراکی cPanel اجرا شود.
**تنها تفاوت دو محیط، فایل `.env` است.**

### مخزن کد — GitHub
- ریپازیتوری حتماً **Private**
- نصب روی سرور با: `git clone` → کپی `.env` → `docker compose up -d`
- آپدیت با `git pull`
- فایل `.env` هرگز کامیت نشود؛ در `.gitignore` قرار گیرد و `.env.example` بدون رمز ارائه شود
- در صورت محدودیت دسترسی به GitHub از سرور: **Gitea** روی همان ESXi به‌عنوان جایگزین داخلی (پیشنهاد شده، هنوز تصمیم‌گیری نشده)

### حالت هاست اشتراکی (بدون Docker)
حداقل الزامات هاست پیش از خرید:
- PHP **۸.۲ یا بالاتر**
- افزونه‌ها: `mbstring`, `intl`, `gd`, `zip`, `bcmath`, `pdo_mysql`
- امکان تغییر Document Root دامنه به پوشه `public` ← **حیاتی**
- امکان Cron Job
- `memory_limit` حداقل ۲۵۶ مگابایت (برای تولید PDF)
- ترجیحاً دسترسی SSH؛ در نبود آن، بسته آماده همراه پوشه `vendor` تحویل شود

تغییرات لازم در این حالت:
- کش و صف روی **دیتابیس** به‌جای Redis
- اجرای کارهای پس‌زمینه با **کرون‌جاب هر دقیقه**

### مدیریت دیتابیس
- **cPanel / DirectAdmin** = پنل مدیریت هاست، **phpMyAdmin** = ابزار مدیریت دیتابیس؛ دو چیز متفاوت‌اند
- سامانه فقط به MySQL/MariaDB نیاز دارد و به هیچ‌کدام از این ابزارها وابسته نیست
- روی هاست اشتراکی، phpMyAdmin در هر دو پنل موجود است
- **نکته DirectAdmin:** ریشه دامنه به‌صورت پیش‌فرض `domains/<domain>/public_html` است و مستقیماً قابل تغییر به `public` نیست → با ساخت symlink یا انتقال محتوای پوشه `public` به `public_html` و اصلاح مسیرها در `index.php` حل می‌شود (باید هنگام نصب انجام شود)
- روی سرور ESXi، phpMyAdmin به‌صورت یک کانتینر جداگانه در `docker-compose` اضافه شود

### محیط تست
- کاربر قصد دارد فاز تست را روی **ویندوز با XAMPP** انجام دهد (پیشنهاد جایگزین: **Laragon**، سبک‌تر و مخصوص Laravel)
- **هشدار:** ویندوز به بزرگی/کوچکی حروف در نام فایل حساس نیست ولی لینوکس هست → نام‌گذاری فایل‌ها و کلاس‌ها از ابتدا با استاندارد لینوکسی
- تست نهایی هر فاز حتماً روی سرور اوبونتو (محیط واقعی) انجام شود

---

## ۳. الزامات کلیدی کاربر (نقل مستقیم خواسته‌ها)

### دسترسی
- سه نقش: **مدیر / کارشناس (کاربر) / مشتری**
- دسترسی‌ها باید **مجوزمحور** باشند، نه نقش ثابت
- برای هر مشتری جداگانه قابل تنظیم: دیدن تاریخچه خدمات، دیدن فاکتور، ثبت تیکت جدید، پرینت فاکتور
- مثال صریح کاربر: «شاید خواستم دسترسی یک مشتری را به تاریخچه ببندم ولی فقط بتواند تیکت جدید بزند»

### وضعیت مشتری
- فعال / معلق / مسدود
- فیلد **متن پیام تعلیق** قابل ویرایش توسط مدیر
- هنگام تلاش برای ثبت تیکت، همان پیام به مشتری نمایش داده شود (مثال کاربر: به دلیل بدهی / قدیمی بودن سامانه)

### خدمات
- دو دسته اصلی: **نرم‌افزاری** و **سخت‌افزاری**
- روش انجام: **تلفنی / ریموت / حضوری** (اعزام نفر)
- دسته‌بندی **دولایه** الزامی است (مثال: سخت‌افزار ← هارد) وگرنه گزارش «بیشترین خرابی» بی‌فایده می‌شود

### قرارداد پشتیبانی
- چند نوع قرارداد قابل تعریف (طلایی، نقره‌ای، برنزی و هر چیز دیگر)
- تاریخ شروع و پایان، درصد یا سقف پوشش، تعیین اینکه چه نوع خدمتی را پوشش می‌دهد
- **مهم:** برای خدمت تحت گارانتی هم فاکتور با مبلغ واقعی صادر شود، ولی **پرداختی مشتری صفر** شود
- مشتری بدون قرارداد → هزینه جداگانه اعمال شود

### فاکتور
- ساده، فقط **PDF داخلی** — بدون مالیات، بدون شماره رسمی، بدون سامانه مودیان
- هر فاکتور سه عدد جدا نگه می‌دارد: **مبلغ خدمت / سهم قرارداد / مبلغ قابل پرداخت**
- مشتری بتواند فاکتورهایش را ببیند و پرینت کند

### رابط کاربری
- زیبایی مهم است، سبک باشد
- سازگار با **موبایل، تبلت، دسکتاپ**

### تم و رنگ‌بندی (انتخاب‌شده)
**تم اصلی: آبی نفتی و فیروزه‌ای**

| نقش | کد رنگ |
|---|---|
| منوی کناری | `#0b2b3f` |
| متن منو | `#93b4c9` |
| رنگ اکشن / برند | `#14b8a6` |
| پس‌زمینه فیروزه‌ای کم‌رنگ | `#ccfbf1` |
| متن روی فیروزه‌ای | `#0f766e` |
| پس‌زمینه صفحه | `#eef4f6` |
| کارت | `#ffffff` |
| خطوط | `#dde8ec` |
| متن اصلی | `#0b2b3f` |
| متن کم‌رنگ | `#5f7d8c` |

**تم شب (حالت دوم — کلید تغییر کنار پروفایل کاربر)**

| نقش | کد رنگ |
|---|---|
| منوی کناری | `#0b1220` |
| پس‌زمینه صفحه | `#111a2b` |
| کارت | `#182338` |
| خطوط | `#25314a` |
| رنگ اکشن / برند | `#34d399` |
| متن اصلی | `#e6edf7` |
| متن کم‌رنگ | `#8ea0b8` |

- انتخاب تم توسط هر کاربر ذخیره شود
- فونت: **وزیرمتن**
- جدول‌ها روی موبایل ستون‌های کم‌اهمیت را پنهان کنند؛ منو به آیکون تبدیل شود

### انعطاف‌پذیری
- کاربر تأکید کرده: «باید خیلی منعطف باشد که بتوانم هی تغییرات رویش اعمال کنم»

---

## ۴. فاز انبار و کالا (الزامات تفصیلی)

### مقیاس
- حدود **۲۰ مدل سامانه**
- هر سامانه تا **۴۰ قطعه مختلف**
- بخشی از تجهیزات در انبار موجود است، بخشی فقط برای همان پروژه خریداری می‌شود و وارد انبار نمی‌شود

### مثال واقعی کاربر — سامانه Titan S2
۳ تلویزیون ۵۵ اینچ، ۵ مانیتور ۲۲ اینچ، ۲ مانیتور ۸ اینچ، ۴ ترک‌بال کوچک، ۱ ترک‌بال بزرگ، ۳ تین‌کلاینت سورین ورژن ۱، ۱ تین‌کلاینت HP Elite، ۱ کیس ویز

### ساختار سه‌سطحی کالا (تصمیم کلیدی)
```
دسته: ترک‌بال
 └ کالا: ترک‌بال کوچک
    ├ ورژن ۴۰۴  ← موجودی ۸
    └ ورژن ۴۰۵  ← موجودی ۱۲
```
- **موجودی و قیمت همیشه روی «ورژن» ثبت می‌شود، نه روی کالا**
- گزارش کلی از جمع ورژن‌ها ساخته می‌شود
- خواسته صریح کاربر: موجودی ترک‌بال کوچک = ۲۰، در جزئیات: ۸ تا ورژن ۴۰۴ و ۱۲ تا ورژن ۴۰۵

### مشخصات فنی
- **قالب مشخصات به ازای هر دسته** (نه جدول ثابت، نه فیلد آزاد بی‌ساختار)
- مثال: دسته «کیس» → CPU / RAM / HDD / GPU — دسته «مانیتور» → اینچ / رزولوشن / پنل
- دلیل: کیس ویز امسال با ASUS 4060 و سال بعد با 5060 اجرا می‌شود

### تفکیک «مدل سامانه» از «سامانه اجراشده» (تصمیم کلیدی)
- **مدل سامانه:** Titan S2 نسخه ۱۴۰۴ → لیست قطعات استاندارد (BOM)
- **سامانه اجراشده:** تایتان S2 نصب‌شده برای مشتری الف → قطعات **واقعی** با ورژن دقیق، سریال، تاریخ نصب، قیمت تمام‌شده همان روز
- نتیجه: سوابق سال‌های قبل دست‌نخورده می‌ماند؛ هنگام تیکت دقیقاً معلوم است چه قطعه‌ای نصب شده

### خرید و واردات
- سند خرید: ارز، مبلغ ارزی، **نرخ یوان و دلار روز حواله**، کرایه حمل، ترخیص، گمرک، بیمه
- هزینه‌های جانبی به نسبت ارزش (یا وزن — قابل انتخاب) سرشکن شود → **قیمت تمام‌شده هر واحد**
- ثبت اینکه هر مدل در چه سالی چند عدد خریداری شده

### لات و FIFO
- هر خرید یک **لات** می‌سازد (۱۲ عدد ورژن ۴۰۵ ممکن است دو لات با دو قیمت باشد)
- خروج از انبار به روش **FIFO**؛ قیمت لات به سامانه می‌چسبد
- خروجی مطلوب: «این Titan S2 با قیمت خرید آن روز، فلان‌قدر تمام شد»

### حرکات انبار
- هر ورود/خروج/انتقال/مرجوعی = یک سند شامل: **کاربر ثبت‌کننده**، تاریخ، مرجع (پروژه/تیکت/خرید)، ورژن کالا، تعداد
- هیچ حرکتی حذف نمی‌شود، فقط سند اصلاحی می‌خورد
- گزارش انبار به تفکیک کاربر (خواسته صریح کاربر)

### چک‌لیست پروژه
- انتخاب مدل سامانه → تولید خودکار لیست قطعات → تطبیق با موجودی
- نمایش: موجود و رزرو شد / کسری: چند عدد باید خریداری شود

---

## ۵. پیشنهادهای پذیرفته‌شده (توسط دستیار مطرح شد)

- **ثبت زمان کارکرد** روی هر تیکت (برای سنجش صرفه اقتصادی هر مشتری)
- **دسته‌بندی دولایه**
- **تاریخچه تغییرات / Audit log**
- **جدول دارایی/سامانه مشتری** و اتصال تیکت به آن
- **رزرو موجودی** (تا دو پروژه همزمان روی یک کالا حساب باز نکنند)
- **سریال و تاریخ گارانتی تأمین‌کننده** برای اقلام گران
- **اتصال تیکت به انبار** — تعویض قطعه از داخل تیکت، خروج انبار می‌خورد و قطعه معیوب به انبار مرجوعی می‌رود → آمار خرابی هر برند خودکار ساخته می‌شود
- **چند انبار** (مرکزی / امانی نزد مشتری / مرجوعی و معیوب)
- **حداقل موجودی و هشدار**
- **قیمت پیشنهادی فروش** بر اساس قیمت تمام‌شده + درصد سود (تولید پیش‌فاکتور)
- **ماژول Import از اکسل** برای انتقال داده‌های فعلی

### پیشنهادهای پذیرفته‌شده و تحویل‌شده (به‌روزرسانی ۱۴۰۵/۰۵/۲۳)
- ✅ SLA و هشدار تیکت معطل — `Ticket::isSlaBreached`، ستون و فیلتر در جدول تیکت‌ها
- ✅ اطلاع‌رسانی ایمیل — پاسخ تیکت، صدور فاکتور، دعوت به نظرسنجی
- ✅ نظرسنجی رضایت پس از حل تیکت — لینک امضاشده، بدون نیاز به ورود
- ✅ زیرساخت پیامک — آماده اتصال، تا انتخاب سرویس واقعی فقط در لاگ می‌نویسد
  (`docs/PROJECT_STATE.md` بخش ۹: انتخاب سرویس پیامک هنوز باز است)

### پیشنهادهای مطرح‌شده ولی هنوز تأیید نشده
پایگاه دانش، بارکد/QR برای انبار.

---

## ۶. چرخه وضعیت تیکت

```
جدید → در حال بررسی → منتظر پاسخ مشتری → منتظر پرداخت → حل‌شده → بسته‌شده
                                                              ↘ لغوشده
```
- تیکت بسته‌شده **قفل می‌شود ولی حذف نمی‌شود** (گزارش‌های تاریخی به آن وابسته است)

---

## ۷. فازبندی اجرا

> کاربر تست هر فاز را انجام می‌دهد و بازخورد می‌دهد. داخل هر فاز، کار بدون سؤال تا انتها پیش می‌رود.

### فازهای این مخزن (سامانه پشتیبانی)

| فاز | محتوا | معیار تست کاربر | وضعیت |
|---|---|---|---|
| **۰** | نصب، اسکلت لاراول، ساختار دیتابیس، تم و ظاهر | ورود به پنل، تأیید ظاهر | ✅ تحویل شد |
| **۱** | مشتری، تیکت، قرارداد، فاکتور PDF، پرتال مشتری، تعلیق | ثبت چند تیکت واقعی، صدور فاکتور، ورود با اکانت مشتری | ✅ تحویل شد |
| **۲** | گزارش‌ها و داشبورد پشتیبانی با خروجی اکسل و PDF | بررسی صحت اعداد درآمد و خدمات | ✅ تحویل شد |

**پس از فاز ۱، سامانه قابل استفاده واقعی است.**

### فازهای مخزن دوم (`Soorin_Inventory`) — اینجا اجرا نمی‌شوند

| فاز | محتوا |
|---|---|
| ۰ | زیرساخت و ساختار دیتابیس انبار |
| ۱ | کالا و ورژن، مشخصات فنی، ورود/خروج، موجودی، چند انبار |
| ۲ | اسناد خرید، ارز، سرشکن هزینه، FIFO، قیمت تمام‌شده |
| ۳ | مدل سامانه، BOM، سامانه اجراشده، چک‌لیست، رزرو |
| ۴ | گزارش‌های انبار و قیمت تمام‌شده |

---

## ۸. گزارش‌های مورد انتظار

### در این سامانه (فاز ۲ همین مخزن)

- درآمد کل و به تفکیک دوره
- حجم خدمات ارائه‌شده (تعداد و زمان کارکرد)
- ارزش خدمات رایگان ارائه‌شده تحت قرارداد ← ابزار مذاکره تمدید قرارداد
- خدمات دریافتی هر مشتری
- آمار خرابی به تفکیک **دسته‌بندی تیکت** (دولایه: سخت‌افزار ← هارد)
- عملکرد کارشناسان و زمان پاسخ

### در مخزن `Soorin_Inventory`

- گردش انبار به تفکیک کاربر
- قیمت تمام‌شده هر سامانه بر اساس نرخ خرید زمان اجرا
- تحلیل اسناد واردات و سرشکن هزینه

### ❌ گزارشی که با تقسیم پروژه از دسترس خارج شد

**«آمار خرابی به تفکیک قطعه و برند»** — این گزارش به اتصال مستقیم تیکت به قطعه انبار نیاز دارد که با تقسیم دو سامانه از بین رفت. جایگزین جزئی: دسته‌بندی دولایه تیکت (بالا) که خرابی را در سطح دسته نشان می‌دهد، نه در سطح برند.

---

## ۹. موارد باز / تصمیم‌گیری نشده

- [x] **نام کسب‌وکار** — شرکت دریاپردازشگران سورین طبرستان
- [x] **رنگ سازمانی** — آبی نفتی و فیروزه‌ای + تم شب (بخش ۳ را ببین)
- [x] لوگو — دریافت شد؛ سه نسخه (سفید/سرمه‌ای/نشان مربعی) + favicon چندسایزی ساخته و در پنل، پرتال و PDF فاکتور جای‌گذاری شد
- [ ] دامنه و نحوه انتشار روی اینترنت (Reverse proxy / SSL)
- [ ] تصمیم درباره پیشنهادهای تأیید‌نشده بخش ۵
- [ ] GitHub یا Gitea داخلی؟
- [ ] محیط استقرار نهایی: سرور ESXi یا هاست اشتراکی؟ (فعلاً هر دو پشتیبانی می‌شود)
- [ ] **انتخاب سرویس پیامک ایرانی** (کاوه‌نگار/ملی‌پیامک/فراپیامک/...) —
  زیرساخت آماده است (`App\Contracts\SmsGateway` + صفحه تنظیمات در پنل)،
  فقط پیاده‌سازی واقعی سرویس انتخابی مانده

---

## ۱۰. گزارش پیشرفت

> هر تغییر با تاریخ در این بخش ثبت شود.

### ۱۴۰۵/۰۵/۲۲
- جمع‌آوری کامل نیازمندی‌ها انجام شد
- استک فنی نهایی شد: Laravel + Filament + MariaDB روی Ubuntu/Docker
- ساختار سه‌سطحی کالا (دسته ← کالا ← ورژن) تصویب شد
- تفکیک «مدل سامانه» از «سامانه اجراشده» تصویب شد
- روش قیمت تمام‌شده: لات + FIFO + سرشکن هزینه‌های وارداتی
- فازبندی ۰ تا ۵ توافق شد
- **تصمیم جدید:** پروژه دوحالته ساخته شود — Docker روی ESXi و نصب مستقیم روی هاست اشتراکی cPanel
- **تصمیم جدید:** کد روی مخزن Private در GitHub با نصب تک‌دستوری
- **تصمیم جدید:** محیط تست اولیه روی ویندوز (XAMPP یا Laragon)
- بخش ۲-ب (استقرار، مخزن کد و محیط تست) به این سند اضافه شد
- **تم انتخاب شد:** آبی نفتی و فیروزه‌ای به‌عنوان تم اصلی + تم شب به‌عنوان حالت دوم؛ کدهای رنگ در بخش ۳ ثبت شد
- **هویت برند نهایی شد:** شرکت دریاپردازشگران سورین طبرستان؛ لوگو دریافت شد؛ سرمه‌ای منو به `#0f2d4d` (سرمه‌ای لوگو) تنظیم شد
- وب‌سایت شرکت: **dpst.ir** — در فوتر، سربرگ فاکتور و ایمیل‌ها استفاده می‌شود
- **فاز ۰ تحویل شد** (بسته `soorin-support-phase0.tar.gz`): ساختار مخزن، `docker-compose` + Dockerfile + Nginx، دو نسخه `.env`، ۸ فایل migration شامل کل ساختار دیتابیس (پایه، مشتری، قرارداد، تیکت، مالی، انبار، خرید و واردات، سامانه و پروژه)، `config/branding.php`، `theme.css` با دو تم، کامپوننت فوتر، اسکریپت نصب و بکاپ، و README نصب برای هر سه محیط
- **مرحله بعد: فاز ۱ — مشتری، تیکت، قرارداد، فاکتور PDF، پرتال مشتری**

### ۱۴۰۵/۰۵/۲۳ (2026-08-14) — فاز ۱ در حال اجرا

- **پروژه به دو سامانه مستقل تقسیم شد** — شرح کامل در بخش «⚠️ تقسیم پروژه» بالای همین سند
- ریپازیتوری دوم `Soorin_Inventory` ساخته شد
- **لاراول ۱۱ قابل نصب نبود** (توصیه‌های امنیتی منتشرشده) → لاراول ۱۲ + Filament v4 نصب شد
- محیط توسعه: Laragon (PHP 8.3، Composer، MySQL 8.4) روی ویندوز
- **دیتابیس با وردپرس dpst.ir مشترک شد** — پیشوند جدول `soorin_` از طریق `DB_TABLE_PREFIX`؛ تصمیم صریح مالک پروژه با پذیرش ریسک امنیتی مرتبط
- **ساختار پروژه‌های مشتری اضافه شد** (`customer_projects` + `customer_project_user`) — یک مشتری چند پروژه دارد (نمونه: شرکت آریا با پروژه‌های بندرعباس/چابهار/بوشهر)؛ تیکت به پروژه وصل می‌شود تا گزارش تفکیکی ممکن باشد
- **چهار نوع حساب کاربری:** `support_admin`, `support_staff`, `customer_admin`, `customer_staff` — دسترسی دولایه (سقف در سطح مشتری، محدودسازی در سطح حساب)؛ فقط مدیر پشتیبان حساب می‌سازد
- بخش ۱ (مدل‌ها) تحویل شد — ۱۴ مدل، منطق دسترسی و محاسبه فاکتور
- بخش ۲ (ورود/نقش/مجوز) تحویل شد — ورود با ایمیل یا موبایل، ۴ نقش، ۱۸ مجوز
- بخش ۳ (پنل Filament) تحویل شد — مشتریان + پروژه‌ها + فونت وزیرمتن محلی
- بخش ۴ (تیکت) تحویل شد — چرخه وضعیت، گفتگو، یادداشت داخلی، ضمیمه، قفل خودکار
- بخش ۵ (قرارداد) تحویل شد — انواع قرارداد، انعقاد، سقف مشترک چند فاکتور بدون دوبار‌شماری، انقضای خودکار شبانه
- بخش ۶ (فاکتور) تحویل شد — صدور از تیکت یا مستقل، PDF فارسی راست‌چین با mPDF و فونت وزیرمتن (TTF جدا از woff2 وب)
- بخش ۷ (پرتال مشتری) تحویل شد — Blade مستقل از پنل، دسترسی از `Ticket::visibleTo` و `User::canCreateTicket/canViewInvoices/canPrintInvoices`
- بخش ۸ (پرداخت‌سازی) تحویل شد — صفحات خطای ۴۰۳/۴۰۴/۴۱۹/۴۲۹/۵۰۰/۵۰۳ فارسی، فوتر در پنل و پرتال
- **۸۵ تست خودکار، همه سبز** (`php artisan test`)
- **فاز ۱ کامل تحویل شد — آماده تست مالک پروژه**

### ۱۴۰۵/۰۵/۲۳ (2026-08-14) — لوگو، رفع دو باگ، سه قابلیت تازه، فاز ۲

- **لوگوی رسمی جای‌گذاری شد** — استخراج پیکسلی از فایل ارسالی مالک پروژه؛
  سه نسخه (navy/white/mark) + favicon چندسایزی؛ نسخه ساده‌شده نشان برای
  سایزهای کوچک (نسخه کامل با سه نوار در ۱۶–۳۲px محو می‌شد)؛ پوشه `Logo/`
  با فایل‌های باکیفیت برای مصارف بیرون از سامانه
- **رفع باگ:** حروف فارسی در PDF فاکتور چسبیده چاپ نمی‌شد (mPDF بدون
  `useOTL` روی فونت سفارشی) — با تنظیم فونت اصلاح و با رندر واقعی تأیید شد
- **رفع باگ:** کلید ترجمه `invoices.amount` خام (بدون ترجمه) در سربرگ
  جدول پرداخت‌های PDF چاپ می‌شد — کشف‌شده با اسکرین‌شات واقعی از فاکتور
- **نظرسنجی رضایت مشتری** — ستون‌های `rating`/`rating_comment` که از
  فاز ۰ آماده بودند فعال شدند؛ لینک امضاشده (بدون ورود) هنگام حل‌شدن تیکت
- **زیرساخت پیامک** — `App\Contracts\SmsGateway` + صفحه تنظیمات در پنل؛
  سرویس واقعی هنوز انتخاب نشده (بخش ۹) پس فقط در لاگ می‌نویسد
- **فاز ۲ (گزارش‌ها) تحویل شد** — درآمد، حجم خدمات، ارزش خدمات رایگان،
  خدمات هر مشتری، آمار خرابی دولایه، عملکرد کارشناسان؛ خروجی اکسل
  (phpoffice/phpspreadsheet) و PDF؛ هر دو با رندر واقعی بازبینی شد
- **۱۳۳ تست خودکار، همه سبز**
- **فاز ۱ و فاز ۲ همین مخزن کامل تحویل شد — آماده تست مالک پروژه.**
  باقی‌مانده: پروژه `Soorin_Inventory` (کاملاً جدا، شروع‌نشده) و استقرار
  روی سرور واقعی

### ۱۴۰۵/۰۶/۱۱ (2026-09-02) — پشتیبان‌گیری، SSL و داشبورد

پورت از سامانهٔ انبار (هر دو از یک قالب ساخته شده‌اند):

- **بخش پشتیبان‌گیری** (`Backups` صفحهٔ پنل): تهیهٔ پشتیبانِ دستی، فهرست/دانلود/حذف،
  بازیابی از فایل یا پشتیبانِ موجود (با پشتیبانِ ایمنیِ خودکار پیش از بازیابی)،
  «بکاپ روی پوشهٔ شبکه (SMB/محلی)» و «زمان‌بندیِ خودکار» (روزانه/هفتگی/ماهانه)،
  و «ضربانِ زمان‌بند» برای تشخیصِ سلامتِ cron/systemd. سرویسِ بکاپ فقط با اتصالِ
  دیتابیس کار می‌کند (بدونِ mysqldump) تا روی هاست اشتراکی هم اجرا شود. با پیشوندِ
  جدولِ `soorin_` سازگار است.
- **پنج مجوزِ جدا برای بکاپ** به `App\Enums\Permission` اضافه شد (view/create/delete/
  restore/settings) — فقط `support_admin`. بازیابی عمداً از تهیهٔ پشتیبان جدا شد.
- **بخش SSL** (`SslSettings` صفحهٔ پنل): self-signed (سرور داخلی) و Let's Encrypt
  (دامنهٔ عمومی) + اجبار HTTPS + غیرفعال‌سازی؛ از راه دستیارِ root `/usr/local/bin/
  soorin-ssl` با قاعدهٔ محدودِ sudoers (پنل با www-data اجرا می‌شود، نه root).
- **داشبورد** غنی‌تر شد: کارت‌های آمار (از قبل) + **نمودارِ روندِ تیکت** (۶ ماه،
  ثبت‌شده/حل‌شده) + **جدولِ آخرین تیکت‌ها** (کلیک‌پذیر به تیکت).
- **اسکریپت‌های استقرار** در `deploy/`: `install-scheduler.sh` (تایمرِ systemd برای
  `schedule:run` هر دقیقه)، `install-ssl-helper.sh` و `soorin-ssl.sh` (نامِ سایتِ
  nginx: `soorin-support`، مسیر: `/var/www/soorin-support`).
- `App\Support\Jalali::TIMEZONE` اضافه شد؛ تیکِ زمان‌بند و بررسیِ بکاپ به
  `routes/console.php` و مسیرِ دانلودِ بکاپ به `routes/web.php` افزوده شد.
- **بخش شخصی‌سازیِ برند** (`BrandingSettings`): `App\Support\Branding` (خواندن از
  settings با fallback به config) + صفحهٔ پنل برای نام/عنوان/اطلاعات تماس/لوگوها
  (light/dark/mark/favicon) + «بازگردانی به پیش‌فرض». دیسکِ `branding` به
  filesystems افزوده شد. نام و لوگوی پنل حالا از Branding خوانده می‌شوند و هدرِ
  فایلِ پشتیبان هم از عنوانِ برند استفاده می‌کند. فقط `support_admin`
  (مجوزِ `ManageSettings`).
- **دسترسیِ همهٔ بخش‌های تازه فقط ادمینِ پشتیبان است:** بکاپ (۵ مجوزِ اختصاصی)،
  SSL و برندینگ (`ManageSettings`) — هیچ‌کدام به `support_staff` داده نشده؛ کاربرانِ
  خریدار (`customer_admin`/`customer_staff`) اصلاً به پنل راه ندارند
  (`User::canAccessPanel` = فقط کاربرِ پشتیبان).
- **۱۳ تستِ جدید** (بکاپ/SSL/داشبورد/برندینگ)؛ کلِ مجموعه **۱۶۶ تست سبز**.
- نسخهٔ برند به `0.4.0` رسید.

### ۱۴۰۵/۰۶/۱۱ (2026-09-02) — تستِ سرتاسری و آماده‌سازیِ امنیتی برای اینترنت (v0.4.1)

- **تستِ end-to-endِ چرخهٔ تیکت** (`TicketLifecycleE2ETest`): کارشناسِ خریدار از
  پرتال تیکت می‌سازد → دیده‌شدن توسط سازنده/مدیرِ خریدار/پشتیبان و **دیده‌نشدن توسط
  مشتریِ دیگر** (پرتال ۴۰۴، نه ۴۰۳، تا وجودِ تیکت فاش نشود) → پاسخِ کارشناسِ پشتیبان
  که مشتری می‌بیند ولی یادداشتِ داخلی را نه → دیدنِ مدیر در پنل → بستن که تیکت را
  قفل و پاسخِ بعدی را ۴۰۳ می‌کند.
- **بازبینیِ امنیتی برای اینترنت** — یافته‌ها و وضعیت:
  - ✅ جداسازیِ دادهٔ مشتری‌ها: `Ticket::scopeVisibleTo` همیشه به `customer_id`
    محدود می‌شود؛ فاکتورها هم در پرتال و PDF به مشتریِ خودش مقید‌اند.
  - ✅ پنل فقط برای کاربرِ پشتیبان؛ بکاپ/SSL/برندینگ فقط ادمینِ پشتیبان.
  - ✅ XSS: هیچ خروجیِ escape‌نشده (`{!! !!}`) نیست. SQLi: فقط Eloquent. CSRF: فعال.
  - ✅ APP_DEBUG=false، بکاپ‌ها بیرونِ webroot با دانلودِ احرازشده و اعتبارسنجیِ نام.
  - ✅ دستیارِ SSL: ورودی‌ها با regex اعتبارسنجی، آرگومان‌ها آرایه‌ای، sudoers محدود.
  - **🔧 رفع‌شد:** ورودِ پرتالِ مشتری throttle نداشت (خطرِ brute-force روی اینترنت).
    حالا ۵ تلاش در دقیقه به‌ازای (شناسه+IP)، صفرشدن پس از ورودِ موفق
    (`PortalLoginThrottleTest`). ورودِ پنلِ Filament از قبل throttle داشت.
  - توصیهٔ استقرار (تنظیمِ .env، نه باگ): `SESSION_SECURE_COOKIE=true` روی HTTPS و
    فعال‌کردنِ «اجبار HTTPS» از صفحهٔ SSL.
- **۷ تستِ تازه**؛ کلِ مجموعه **۱۷۳ تست سبز**. نسخهٔ برند `0.4.1`.

### ۱۴۰۵/۰۶/۱۱ (2026-09-02) — تستِ ماتریسیِ جداسازیِ چند-مستأجری (v0.4.2)

- `MultiTenantVisibilityTest`: چند مشتری، چند پروژه، چند کارشناس با دامنه‌های
  متفاوت (`customer`/`project`/`own`). تأییدِ کامل که:
  - مدیرِ خریدار فقط تیکت‌های سازمانِ خودش را می‌بیند، نه مشتریِ دیگر را.
  - کارشناسِ `own` فقط تیکتِ ساختهٔ خودش را می‌بیند، نه تیکتِ کارشناسِ دیگر.
  - کارشناسِ `project` فقط تیکت‌های پروژه‌های تخصیص‌یافته‌اش را می‌بیند.
  - جداسازیِ کاملِ داده بینِ مشتری‌ها (هیچ نشتی).
  - ادمین و کارشناسِ پشتیبان همهٔ تیکت‌ها را می‌بینند.
  - پرتال هم همین ماتریس را اعمال می‌کند (۲۰۰ برای مجاز، ۴۰۴ برای بقیه).
- **۶ تستِ تازه**؛ کلِ مجموعه **۱۷۹ تست سبز**. نسخهٔ برند `0.4.2`.

### ۱۴۰۵/۰۶/۱۱ (2026-09-02) — راهنمای نصب + ایمنیِ بکاپ در دیتابیسِ مشترک (v0.4.3)

- **`docs/INSTALL.md`**: راهنمای کاملِ نصب — سرور اختصاصی (بدون/با Docker) و هاستِ
  اشتراکیِ DirectAdmin (زیردامنه، جداکردنِ public با symlink، دیتابیسِ مشترک با
  `DB_TABLE_PREFIX`، کرونِ زمان‌بند، SSL)، به‌علاوه **روشِ بدونِ SSH (vendor+import)**
  و بخشِ **به‌روزرسانی** (تک‌دستوری با `deploy/update.sh`، کرونِ شبانه، یا webhook).
- **`deploy/update.sh`**: به‌روزرسانیِ تک‌دستوری (git pull → composer → migrate → optimize:clear).
- **🔧 رفعِ ایمنیِ مهم برای دیتابیسِ مشترک:** `DatabaseBackupService::tables()` حالا
  وقتی `DB_TABLE_PREFIX` تنظیم شده باشد فقط جدول‌های همان پیشوند را بکاپ/بازیابی
  می‌کند. پیش از این، بکاپ از **کلِ** دیتابیس گرفته می‌شد و بازیابی می‌توانست جدول‌های
  نرم‌افزارِ دیگر (وردپرس) را حذف/بازنویسی کند. بدونِ پیشوند، رفتارِ قبلی (کلِ
  دیتابیس) حفظ می‌شود. تستِ اختصاصی افزوده شد.
  > همین باگ در سامانهٔ انبار هم هست (سرویسِ یکسان)؛ هنگام ادامهٔ آن پروژه باید اعمال شود.
- **۱ تستِ تازه**؛ کلِ مجموعه **۱۸۰ تست سبز**. نسخهٔ برند `0.4.3`.

### ۱۴۰۵/۰۶/۱۲ (2026-09-03) — به‌روزرسانیِ تک‌کلیکیِ داخلِ برنامه (v0.4.4)

- **پورتِ سیستمِ به‌روزرسانیِ داخلِ پنل از سامانهٔ انبار** (مثلِ وردپرس): صفحهٔ
  «به‌روزرسانی» با دکمهٔ تک‌کلیکِ آپدیت از گیت‌هاب، آپدیت با فایلِ zip، «اتصال به
  گیت‌هاب» برای نصب‌های زیپی، بررسیِ نسخه، و نشانِ قرمزِ «نسخهٔ جدید» در منو.
  پیش از هر آپدیت، پشتیبانِ ایمنیِ خودکار گرفته می‌شود.
- اجزا: `App\Support\AppVersion` (خواندن از فایلِ `VERSION`)، `AppUpdateService`،
  صفحهٔ `AppUpdate`، دستورهای `soorin:check-update` (روزانه) و `soorin:link-github`،
  نشانِ منو، و `github.repo` در `config/branding.php`. فوتر و کش‌شکنِ theme.css هم
  به فایلِ `VERSION` وصل شدند. فقط `support_admin` (مجوزِ `ManageSettings`).
- **⚠️ محدودیت:** این آپدیتِ تک‌کلیکی `git pull`/`composer`/`migrate` را با
  proc_open اجرا می‌کند؛ روی **سرورِ اختصاصی/VPS** عالی کار می‌کند ولی روی هاستِ
  اشتراکیِ بدونِ SSH که exec غیرفعال است معمولاً کار نمی‌کند — آنجا باید از روشِ
  vendor+import یا `deploy/update.sh` استفاده شود.
- **۳ تستِ تازه**؛ کلِ مجموعه **۱۸۳ تست سبز**. نسخه از فایلِ `VERSION` = `0.4.4`.
