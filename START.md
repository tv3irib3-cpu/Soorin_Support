# START — دستور کار Claude Code

> **این فایل تنها ورودی توست.** از بالا تا پایین، مرحله به مرحله اجرا کن.
> جز در نقاطی که صریحاً نوشته شده «اینجا توقف کن»، سؤال نپرس و تا انتها برو.

پروژه: سامانه خدمات و پشتیبانی **شرکت دریاپردازشگران سورین طبرستان** (dpst.ir)
مالک پروژه فارسی‌زبان است؛ همه پاسخ‌ها و پیام‌های کامیت به فارسی باشد.

---

## مرحله ۱ — خواندن مستندات

این چهار فایل را به همین ترتیب بخوان:

1. `CLAUDE.md` — قواعد پروژه. در تمام مدت کار رعایتشان کن.
2. `docs/PROJECT_STATE.md` — حافظه پروژه: نیازمندی‌ها، تصمیم‌ها و دلایلشان.
3. `docs/PHASE-1-BRIEF.md` — شرح کار فازی که باید انجام دهی.
4. `README.md` — روش نصب در سه محیط.

سپس فایل‌های موجود را فهرست کن و در **حداکثر ۱۰ خط** گزارش بده چه چیزی آماده است.
گزارش طولانی نده و مستندات را برای مالک پروژه بازگو نکن؛ او آن‌ها را نوشته است.

---

## مرحله ۲ — نصب اسکلت لاراول

اسکلت لاراول هنوز نصب نیست (نه `composer.json` هست، نه `artisan`، نه پوشه `app`).
لاراول ۱۱ را در همین پوشه بیاور، **بدون بازنویسی فایل‌های موجود**.

روش امن:
1. لاراول را در یک پوشه موقت بیرون از پروژه بساز.
2. فقط فایل‌ها و پوشه‌هایی را منتقل کن که در پروژه وجود ندارند.
3. برای فایل‌هایی که در هر دو طرف هستند، نسخه پروژه اولویت دارد.

این‌ها باید دست‌نخورده بمانند:

```
CLAUDE.md   START.md   README.md   docker-compose.yml   docker/
config/branding.php
database/migrations/          (هر ۸ فایل)
resources/css/theme.css
resources/views/components/footer.blade.php
scripts/   docs/   lang/fa/   public/images/
.gitignore   .gitattributes   .editorconfig
.env.example   .env.shared-host.example
```

اگر تداخل غیرمنتظره‌ای دیدی، **اینجا توقف کن** و بپرس.

### بسته‌های موردنیاز

```
filament/filament
spatie/laravel-permission
mpdf/mpdf
hekmatinasser/verta
```

فونت **وزیرمتن** را به‌صورت محلی در `public/fonts/` قرار بده و در `theme.css`
با `@font-face` معرفی کن. از CDN استفاده نکن — سرور ممکن است دسترسی نداشته باشد.
همین فونت برای mPDF هم تنظیم شود تا فاکتور فارسی درست چاپ شود.

### راه‌اندازی محیط

```
cp .env.example .env
php artisan key:generate
php artisan migrate
```

اگر migration خطا داد، **رفعش کن** — این جدول‌ها ستون فقرات پروژه‌اند.
اگر نصب بسته‌ها به خطای شبکه خورد، از داخل کانتینر Docker امتحان کن و اگر باز
هم نشد **اینجا توقف کن** و به مالک پروژه بگو.

---

## مرحله ۳ — گیت

```
git init
git branch -M main
```

قبل از کامیت با `git status --short` مطمئن شو `.env` در فهرست نیست.
کامیت اول:

```
chore: راه‌اندازی اسکلت پروژه — فاز ۰
```

remote هنوز اضافه نشود؛ مالک پروژه بعداً آدرس مخزن Private را می‌دهد.

---

## مرحله ۴ — اجرای فاز ۱

طبق `docs/PHASE-1-BRIEF.md` کامل جلو برو. بعد از هر بخش یک کامیت بزن:

| # | بخش | پیام کامیت |
|---|---|---|
| ۱ | مدل‌ها و روابط | `models: مدل‌های پایه و روابط` |
| ۲ | نقش‌ها، مجوزها، ورود | `auth: ورود، نقش‌ها و مجوزها` |
| ۳ | پنل Filament: مشتریان، کاربران، تنظیمات | `panel: مدیریت مشتریان و کاربران` |
| ۴ | تیکت‌ها: چرخه وضعیت، گفتگو، یادداشت داخلی، ضمیمه | `tickets: چرخه کامل تیکت` |
| ۵ | قراردادها | `contracts: انواع قرارداد و پوشش` |
| ۶ | فاکتور + خروجی PDF فارسی | `invoices: صدور فاکتور و PDF` |
| ۷ | پرتال مشتری | `portal: پرتال مشتری و دسترسی‌ها` |
| ۸ | Seeder، صفحات خطا، فوتر | `polish: داده نمونه و صفحات خطا` |

بعد از هر کامیت، بخش «گزارش پیشرفت» در `docs/PROJECT_STATE.md` را با یک خط
به‌روز کن. در پایان فاز، وضعیت فاز ۱ را در جدول فازها به ✅ تغییر بده.

### قواعدی که در تمام هشت بخش باید رعایت شوند

- تمام متن رابط کاربری فارسی و در `lang/fa/`، نه هاردکد در Blade.
- رنگ‌ها فقط از `config/branding.php` و متغیرهای CSS در `resources/css/theme.css`.
  هیچ رنگ هاردکدی. دو تم `ocean` و `night` هر دو باید کار کنند.
- مبالغ به ریال و به‌صورت عدد صحیح. هرگز اعشاری.
- تاریخ در دیتابیس میلادی، در نمایش شمسی.
- فاکتور همیشه سه عدد جدا: ارزش خدمت، سهم قرارداد، مبلغ قابل پرداخت.
  خدمت تحت گارانتی هم فاکتور با مبلغ واقعی می‌گیرد و پرداختی صفر می‌شود.
- تیکت بسته‌شده قفل می‌شود، حذف نمی‌شود. همه‌جا SoftDelete.
- دسترسی مشتری علاوه بر نقش، از فیلدهای خود مشتری خوانده شود:
  `can_create_ticket`, `can_view_history`, `can_view_invoices`,
  `can_print_invoices`, `service_status`, `suspension_message`.
- نام فایل و کلاس دقیقاً PSR-4. مالک پروژه روی ویندوز تست می‌کند، سرور لینوکس است.
- هر صفحه روی موبایل کار کند: ستون‌های کم‌اهمیت جدول پنهان شوند.
- فوتر (`resources/views/components/footer.blade.php`) در همه صفحات، شامل
  کپی‌رایت با سال شمسی پویا، لینک dpst.ir و شماره نسخه.

---

## مرحله ۵ — تحویل

در پایان فاز ۱، در حداکثر ۱۵ خط بنویس:

1. دستور دقیق اجرای پروژه روی ویندوز (XAMPP/Laragon)
2. نام کاربری و رمز اکانت‌های نمونه (مدیر، کارشناس، مشتری)
3. فهرست کوتاه چیزهایی که مالک پروژه باید تست کند
4. هر چیزی که عمداً انجام نشده و به فاز بعد موکول شده

---

## چیزهایی که نباید انجام دهی

- تغییر ساختار جدول‌های `database/migrations/` بدون پرسیدن.
- افزودن وابستگی سنگین یا قابلیتی که روی هاست اشتراکی کار نمی‌کند
  (Redis اجباری، WebSocket، پردازش پس‌زمینه دائمی).
- حذف داده تاریخی به هر بهانه.
- کامیت کردن `.env` یا هر رمزی.
- ساختن فایل مستندات جدید. مستندات موجود را به‌روز کن، فایل تازه نساز.

---

## اگر جلسه بسته شد

جلسه جدید را با همین جمله شروع کن:
«`CLAUDE.md` و `docs/PROJECT_STATE.md` را بخوان و از آخرین وضعیت ثبت‌شده ادامه بده.»
