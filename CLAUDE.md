# راهنمای کار روی این پروژه (Claude Code)

اگر تازه شروع کرده‌ای، ابتدا `START.md` را بخوان — دستور کار مرحله‌به‌مرحله آنجاست.

این فایل را در ابتدای هر جلسه بخوان. سپس `docs/PROJECT_STATE.md` را بخوان تا
نیازمندی‌های کامل، تصمیم‌های گرفته‌شده و فاز جاری را بدانی.

---

## پروژه چیست

سامانه تحت وب خدمات و پشتیبانی برای **شرکت دریاپردازشگران سورین طبرستان** (dpst.ir):
ثبت تیکت، صدور فاکتور، قرارداد پشتیبانی، انبار و واردات، مدیریت پروژه و سامانه‌های نصب‌شده.

هدف نهایی مالک پروژه **گزارش‌گیری** است: چقدر خدمات داده، چقدر درآمد داشته،
هر مشتری چه گرفته، بیشترین خرابی کجا بوده، و هر سامانه چقدر تمام شده.
هر تصمیم فنی باید این هدف را تقویت کند.

## استک

Laravel 11 · Filament · MariaDB 11 · Nginx · Redis · Docker
PDF با mPDF و فونت وزیرمتن · تاریخ شمسی (hesabro/hesabix یا verta)

## وضعیت فعلی

فاز ۰ تحویل شده: زیرساخت + ساختار کامل دیتابیس + تم.
**فاز بعدی: فاز ۱** — شرح کامل در `docs/PHASE-1-BRIEF.md`.

---

## قواعدی که نباید بشکنی

1. **موجودی و قیمت روی `item_versions` ثبت می‌شود، نه روی `items`.**
   موجودی کل کالا = جمع ورژن‌هایش. هرگز ستون موجودی به `items` اضافه نکن.

2. **`system_versions` (نقشه) از `customer_systems` (سامانه اجراشده) جداست.**
   تغییر نقشه نباید سوابق نصب‌شده گذشته را عوض کند.

3. **هیچ رکورد `stock_movements` حذف یا ویرایش نمی‌شود.** اصلاح فقط با سند معکوس.

4. **قیمت تمام‌شده از `stock_lots` با FIFO خوانده می‌شود** و در لحظه مصرف روی
   `customer_system_parts.unit_cost` ثبت (منجمد) می‌شود.

5. **تیکت بسته‌شده قفل می‌شود ولی حذف نمی‌شود** (`is_locked`). گزارش‌های
   تاریخی به آن وابسته‌اند. همه‌جا از SoftDelete استفاده کن، نه حذف واقعی.

6. **فاکتور همیشه سه عدد جدا دارد:** `service_amount` (ارزش واقعی خدمت)،
   `contract_amount` (سهم قرارداد/گارانتی)، `payable_amount` (پرداختی مشتری).
   خدمت تحت گارانتی، فاکتور با مبلغ واقعی می‌گیرد و پرداختی صفر می‌شود.
   هرگز برای خدمت گارانتی مبلغ را صفر ثبت نکن — ارزش خدمت باید در گزارش دیده شود.

7. **دسترسی مشتری، نه فقط بر اساس نقش بلکه بر اساس فیلدهای خود مشتری** بررسی
   می‌شود: `can_create_ticket`, `can_view_history`, `can_view_invoices`,
   `can_print_invoices`, و `service_status` + `suspension_message`.
   هنگام مسدود بودن، پیام سفارشی همان مشتری نمایش داده شود.

8. **هر تغییر مهم در `activity_logs` ثبت شود** (چه کسی، کِی، چه چیزی).

## قواعد نوشتن کد

- نام فایل و کلاس دقیقاً PSR-4. مالک پروژه روی **ویندوز** تست می‌کند ولی سرور
  **لینوکس** است؛ اشتباه در بزرگی/کوچکی حروف روی سرور خطا می‌دهد.
- تمام متن‌های رابط کاربری **فارسی** و در فایل‌های زبان (`lang/fa/`) — نه هاردکد در Blade.
- جهت صفحه RTL، فونت وزیرمتن، اعداد فارسی در نمایش و انگلیسی در دیتابیس.
- مبالغ به **ریال** و به‌صورت عدد صحیح (`unsignedBigInteger`) ذخیره می‌شوند — هرگز float.
- تاریخ‌ها در دیتابیس میلادی، در نمایش شمسی.
- رنگ‌ها فقط از `config/branding.php` و متغیرهای CSS خوانده شوند — هیچ رنگ هاردکدی.
- هر صفحه باید روی موبایل کار کند: ستون‌های کم‌اهمیت جدول پنهان شوند.
- قبل از افزودن ستون یا جدول جدید، `docs/PROJECT_STATE.md` را چک کن؛ احتمالاً
  از قبل پیش‌بینی شده است.

## کامیت و گیت

- ریپازیتوری **Private** بماند. `.env` هرگز کامیت نشود.
- پیام کامیت فارسی یا انگلیسی، ولی با پیشوند حوزه:
  `tickets: افزودن یادداشت داخلی` · `inventory: محاسبه FIFO` · `docs: به‌روزرسانی وضعیت`
- هر کامیت یک تغییر منطقی. migration و مدل مربوط به آن در یک کامیت.
- روی شاخه `main` مستقیم کار نکن اگر تغییر بزرگ است؛ شاخه `feature/<نام>` بساز.

## بعد از هر پیشرفت

**بخش «گزارش پیشرفت» در `docs/PROJECT_STATE.md` را به‌روز کن** و وضعیت فاز را
در جدول فازها تغییر بده. این فایل حافظه پروژه است و در جلسه بعدی مبنا قرار می‌گیرد.

## چیزهایی که نباید انجام دهی

- افزودن وابستگی سنگین بدون دلیل روشن (پروژه باید روی هاست اشتراکی هم اجرا شود).
- استفاده از قابلیت‌هایی که روی هاست اشتراکی کار نمی‌کنند بدون جایگزین
  (مثلاً Redis اجباری، WebSocket، پردازش پس‌زمینه دائمی).
- تغییر ساختار جدول‌های تحویل‌شده فاز ۰ بدون هماهنگی با مالک پروژه.
- حذف داده‌های تاریخی به هر بهانه.

## قواعد استقرار و نصب روی هاست اشتراکی (LiteSpeed/DirectAdmin) — این‌ها را نشکن

این‌ها از یک عیب‌یابیِ طولانیِ واقعی به‌دست آمده‌اند. شکستنِ هرکدام یعنی نصب یا ورودِ
کاربر روی هاستِ اشتراکی دوباره خراب می‌شود. سرِ هر آپدیت این فهرست را چک کن.

1. **`DB_HOST` باید `localhost` باشد، نه `127.0.0.1`.** این هاست‌ها اتصالِ TCP به پورت
   ۳۳۰۶ را بسته‌اند («Establishing tcp connections on remote port 3306 has been
   disabled»); فقط سوکتِ یونیکس (`localhost`) کار می‌کند. پیش‌فرضِ فرمِ نصب و
   `.env.shared-host.example` باید `localhost` بماند.

2. **درایورِ نشست و کش باید `file` باشد (نه `database`)** در `.env.shared-host.example`
   (`SESSION_DRIVER=file`, `CACHE_STORE=file`, `QUEUE_CONNECTION=sync`). نصب‌کننده باید
   پیش از ساختِ جدول‌ها بالا بیاید؛ با درایورِ دیتابیسی، `/install` قبل از وجودِ جدولِ
   `sessions` خطای ۵۰۰ می‌دهد.

3. **`APP_KEY` باید پیش از هر صفحهٔ وب موجود باشد.** با کلیدِ خالی، میدلورِ
   `EncryptCookies` هر صفحه — حتی `/install` — را ۵۰۰ می‌کند. `EnsureAppKeyServiceProvider`
   این را هنگامِ بوت خودکار می‌سازد؛ **هرگز حذفش نکن** و از اولِ `bootstrap/providers.php`
   برندار.

4. **هر چیزی که در مرورگر بار می‌شود باید فایلِ فیزیکی باشد، نه مسیرِ زنده با پسوندِ
   استاتیک.** این هاست‌ها فایلِ `.js/.css`ِ فیزیکی را سرو می‌کنند ولی مسیرِ زنده‌ای که به
   `.js` ختم شود (مثلِ `/livewire/livewire.js`) را ۴۰۴ می‌دهند → هستهٔ Livewire بار
   نمی‌شود، فرمِ Filament ارسال نمی‌شود و صفحه فقط ری‌لود می‌شود. بنابراین:
   - فایل‌های Livewire باید **publish** شوند (`public/vendor/livewire/`) تا Livewire خودکار
     از `/vendor/livewire/livewire.js` (فایلِ فیزیکی) استفاده کند. این در
     `post-autoload-dump`ِ composer هست (`vendor:publish --tag=livewire:assets --force`) —
     **حذفش نکن.**
   - assetهای Filament هم باید publish و در `public/` کامیت باشند.
   - **بعد از هر آپدیتِ Livewire/Filament** دوباره publish کن و مطمئن شو
     `/vendor/livewire/livewire.js` (۲۰۰) و فرمِ ورود کار می‌کنند.

5. **کلِ محتوای `public/` باید در `public_html/` قرار گیرد** (وب‌روتِ این هاست‌ها
   `public_html` است و `APP_PUBLIC_PATH=public_html`). فقط `index.php` کافی نیست؛
   `js/`, `css/`, `fonts/`, `images/`, `vendor/` هم باید آنجا باشند. بستهٔ نصب باید همه را
   داشته باشد.

6. **`Schema::defaultStringLength(191)` در `AppServiceProvider`** برای هاست‌های قدیمی
   (MyISAM، خطای «key too long») لازم است — حذفش نکن.

7. **آزمونِ سلامتِ نصب پیش از هر ریلیز:** روی نصبِ تازه با `.env.shared-host.example`
   (کلیدِ خالی، دیتابیسِ خالی) باید `/install` با ۲۰۰ بالا بیاید؛ و پس از نصب،
   `/admin/login` باید `window.Livewire` را تعریف کند و فرم واقعاً به سرور ارسال شود
   (نه ری‌لودِ خالی).
